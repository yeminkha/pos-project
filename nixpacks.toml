[variables]
NIXPACKS_PHP_ROOT_DIR="/"

[phases.setup]
aptPkgs = ["bison", "autoconf", "re2c"]

[phases.build]
cmds = [
  "composer install --no-dev --optimize-autoloader",
  "npm install",
  "npm run build"
]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
